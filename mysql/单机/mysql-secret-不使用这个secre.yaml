# apiVersion: v1
# kind: Secret
# metadata:
#   name: password-secrets-xxxxx废弃
#   namespace: infra
# type: Opaque
# data:
#   # echo  -n  root | base64     必须加 -n 否认环境变量会换行【这种方式也不行】
#   # 需要使用这种重定向到文件中 kubectl create secret generic  password-secrets --from-literal=mysql_root_username=root --from-literal=mysql_root_password=root123 -n infra --dry-run=client -oyaml
#   mysql_root_username: cm9vdA==
#   mysql_root_password: cm9vdDEyMw==